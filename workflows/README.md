# Workflows Directory

Place official workflow manifests here.

Each workflow referenced in `catalog.json` must:
- exist at the listed `manifest_path`
- include declared `permissions`
- pass `scripts/validate_catalog.py`

# Messaging Connectors (M4)

Planned first-party connectors:
- Telegram (first)
- iMessage
- Signal (when supported)

Rules:
- Declare provider-specific write permission for send operations
- Include dependency checks (for example `signal-cli`)
- Include explicit channel setup steps
